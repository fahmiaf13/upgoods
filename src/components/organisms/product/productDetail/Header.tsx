import React, { useEffect, useState } from "react";
import axios from "axios";

interface Props {
  products: any;
}

const Header = (props: Props) => {
  const { products } = props;

  return <section className="">sss</section>;
};

export default Header;
